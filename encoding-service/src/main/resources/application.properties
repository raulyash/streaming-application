spring.application.name=encoding-service

spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=encoding-service-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer

spring.kafka.consumer.properties.spring.json.trusted.packages="*"
spring.kafka.consumer.properties.spring.json.use.type.header=false
spring.kafka.consumer.properties.spring.json.value.default.type=com.netflix.encodingService.event.VideoUploadEvent

spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

server.port=8003

# AWS config
aws.access-key="${AWS_ACCESS_KEY:}"
aws.secret-key="${AWS_SECRET_KEY:}"
aws.region="${AWS_REGION:}"

aws.s3.bucket-name=netflix-streaming-videos-528730633309-eu-north-1-an

# actuator
management.endpoints.web.exposure.include=health, info

# FFmpeg config
ffmpeg.path: ${FFMPEG_PATH:ffmpeg}